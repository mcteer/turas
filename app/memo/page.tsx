import { ProtectedShell } from "@/app/_components/protected-shell";
import { calculateServicesPlan, DEFAULT_SERVICES_PLAN } from "@/lib/demo/services-plan";
import { getEngagementBrief } from "@/lib/demo/repository";
import { engagementMetrics } from "@/lib/demo/metrics";

export default function MemoPage() {
  const annual = calculateServicesPlan(DEFAULT_SERVICES_PLAN);
  const pilot = getEngagementBrief("eng-notion-pilot");
  const replacement = pilot && engagementMetrics(pilot, { remainingHours: 40 });
  const additive = pilot && engagementMetrics(pilot, { remainingHours: 120 });
  const currency = (value: number) => new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 }).format(value);
  return <ProtectedShell returnTo="/memo"><main className="mx-auto max-w-3xl p-6 pt-16 md:pt-10"><p className="text-sm font-medium text-amber-800">Synthetic interview demo</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Why this weekly operating review matters</h1><div className="mt-7 space-y-6 rounded-xl border bg-card p-6 text-sm leading-6"><p>The first-90-days priority is a weekly decision about where to intervene before delivery risk becomes a missed customer outcome or an unplanned fixed-fee overrun. The review makes scope, specialist capacity, enablement ownership, and decision deadlines visible together.</p><p>Scope control and timely customer decisions protect the relationship and the engagement contribution forecast. Staffing fit protects delivery quality and avoids treating aggregate free hours as proof that the constrained skill is available. Utilization and capacity remain planning inputs, not individual performance scores.</p><p><strong>Commercial case (invented assumptions):</strong> Three delivery staff × 46 weeks × 40 hours × 65% billable utilization × $250 realized hourly rate yield {currency(annual.revenue)} annual revenue. Full payroll, overhead and non-labor costs of {currency(annual.totalCost)} leave {currency(annual.operatingContribution)} operating contribution ({annual.operatingMarginPercent}%). Break-even utilization is {annual.breakEvenUtilizationPercent}%; a 20% target margin requires about ${annual.targetRate}/hour at the same utilization. These are planning assumptions, not Vercel results.</p>{pilot?.metrics.status === "known" && replacement?.status === "known" && additive?.status === "known" ? <p>The fictional Alderwick engagement illustrates the weekly decision: its $60,000 fee produces {currency(pilot.metrics.contribution)} allocated contribution. Replacing 80 remaining hours with 40 yields {currency(replacement.contribution)}; adding 40 instead yields {currency(additive.contribution)}. The enablement lead has only four schedulable hours this week. Approval therefore needs scope acceptance and a feasible staffing plan. Released effort is not automatically a cash saving, and engagement contribution excludes annual unallocated costs.</p> : null}<p><strong>Assumptions to pressure-test in 30 days:</strong> fictional fee, loaded costs, effort forecasts, utilization denominator, skill bottlenecks, customer ownership, scope-change frequency, and the proposed adoption baseline. No figure is a Vercel benchmark or financial statement.</p><p><strong>Next:</strong> validate the pilot’s operating-owner acceptance, compare forecast to observed effort and adoption evidence, and extend the workflow only when those measures justify it.</p><h2 className="pt-2 text-lg font-semibold">Deliberate Vercel architecture</h2><p><strong>AI SDK and Eve</strong> provide the streamed, durable assistant interface and typed, approval-gated tools. <strong>AI Gateway</strong> supplies the selected model route and model observability without embedding a provider key in the application. <strong>Vercel Workflows</strong>, used by Eve, make approvals and research delegation resumable instead of depending on an open browser request. The Next.js application and server-side session cookie provide the reviewer-facing product boundary.</p><pre className="overflow-x-auto rounded-md bg-muted p-4 text-xs">Reviewer -&gt; signed app cookie -&gt; Next.js app shell -&gt; fictional scenarios + public evidence + Postgres metadata{"\n"}                         -&gt; Eve / AI SDK -&gt; AI Gateway{"\n"}Eve -&gt; typed tools / approval-gated decision / research specialist{"\n"}Eve durable sessions and approvals -&gt; Vercel Workflows</pre><h2 className="pt-2 text-lg font-semibold">20-minute walkthrough</h2><ol className="list-decimal space-y-2 pl-5"><li>Sign in and open the portfolio exception.</li><li>Inspect the synthetic engagement evidence and deterministic 80-hour versus 40-hour scenario.</li><li>Ask Turi for a grounded brief; request the cited implementation/adoption research proposal.</li><li>Approve or reject the single synthetic intervention and refresh while waiting.</li><li>Return to history, show the memo, then discuss the unknown-evidence path.</li></ol></div></main></ProtectedShell>;
}
