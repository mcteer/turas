"use client";
import { useMemo, useState } from "react";
import { engagementMetrics } from "@/lib/demo/metrics";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
export function ScenarioControls({ engagementId, baselineFee, baselineRemainingHours }: { engagementId: string; baselineFee: number; baselineRemainingHours: number }) { const [remaining, setRemaining] = useState(baselineRemainingHours); const [fee, setFee] = useState(baselineFee); const result = useMemo(() => engagementMetrics({ fee: baselineFee, actualHours: 240, remainingHours: baselineRemainingHours, loadedHourlyCost: 125, nonLaborCost: 3000 } as never, { remainingHours: remaining, fee }), [remaining, fee, baselineFee, baselineRemainingHours]); return <section className="mt-7 rounded-xl border bg-card p-5"><h2 className="font-semibold">What-if preview</h2><p className="mt-1 text-sm text-muted-foreground">Hypothetical only; this does not alter the synthetic record.</p><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Remaining effort (hours)<Input min="0" onChange={(e) => setRemaining(Number(e.target.value))} type="number" value={remaining} /></label><label className="text-sm font-medium">Hypothetical fee ($)<Input min="1" onChange={(e) => setFee(Number(e.target.value))} type="number" value={fee} /></label></div>{result.status === "known" ? <p className="mt-4 text-sm">Forecast contribution: <strong>{new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" }).format(result.contribution)}</strong> ({result.contributionPercent}%). Metric version {result.metricVersion}.</p> : <p className="mt-4 text-sm text-destructive">Inputs are invalid.</p>}<p className="mt-3 text-xs text-muted-foreground">Engagement {engagementId}; start date and skill allocation remain visible in the proposed intervention above.</p></section>; }
