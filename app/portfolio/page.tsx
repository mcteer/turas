import Link from "next/link";
import { ProtectedShell } from "@/app/_components/protected-shell";
import { getPortfolioSummary } from "@/lib/demo/repository";
export default async function PortfolioPage() { const portfolio = getPortfolioSummary(); return <ProtectedShell returnTo="/portfolio"><main className="mx-auto max-w-6xl p-6 pt-16 md:pt-8"><p className="text-sm font-medium text-amber-800">Synthetic interview demo · reporting date {portfolio.reportingDate} · data {portfolio.dataVersion}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Weekly delivery operating review</h1><p className="mt-2 max-w-2xl text-muted-foreground">Where should we intervene this week to protect the customer outcome and delivery contribution without overcommitting the team?</p><div className="mt-7 grid gap-4 sm:grid-cols-3"><Metric label="Forecast revenue" value={currency(portfolio.forecastRevenue)} /><Metric label="Forecast contribution" value={currency(portfolio.forecastContribution)} /><Metric label="Capacity pressure" value={`${portfolio.capacity.filter((person) => person.metrics.isConstrained).length} constrained skill lane(s)`} /></div><section className="mt-8 rounded-xl border bg-card"><div className="border-b p-5"><h2 className="font-semibold">Exceptions needing a decision</h2><p className="mt-1 text-sm text-muted-foreground">All figures are deterministic planning assumptions, not financial statements.</p></div><div className="divide-y">{portfolio.exceptions.map((engagement) => <Link className="block p-5 transition hover:bg-accent/50" href={`/engagements/${engagement.id}`} key={engagement.id}><div className="flex items-start justify-between gap-5"><div><h3 className="font-medium">{engagement.name}</h3><p className="mt-1 text-sm text-muted-foreground">{engagement.risk}</p></div><span className="text-sm font-medium">Review →</span></div></Link>)}</div></section></main></ProtectedShell>; }
function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-xl border bg-card p-5"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p></div>; }
function currency(value: number) { return new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 }).format(value); }
